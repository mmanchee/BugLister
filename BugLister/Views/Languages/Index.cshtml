@{
  Layout = "_Layout";
}

@using BugLister.Models;
@model List<BugLister.Models.Language>;

<h1>Languages</h1>

<div class="box">
  <div class="inner-box">
    @if (@Model.Count == 0)
    {
      <p>No languages have been added yet!</p>
    }
    @foreach (Language language in Model)
    {
      <li>@Html.ActionLink($"{language.LanguageName}", "Details", new { id = language.LanguageId })</li>
    }
  </div>  
</div>

<p>@Html.ActionLink("Add new language", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>